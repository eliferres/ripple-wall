# Models

The roster in `config/model-roster.json` is the only place a model is
defined. This page is a copy for readers, and CI keeps a third copy.

Currently allowed:

- `atlas-mini` — planning and routing.
- `atlas-large` — review and long-context reading.
